import { TabcomponentPipe } from './tabcomponent.pipe';

describe('TabcomponentPipe', () => {
  it('create an instance', () => {
    const pipe = new TabcomponentPipe();
    expect(pipe).toBeTruthy();
  });
});
