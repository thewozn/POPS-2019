import { ValcomponentPipe } from './valcomponent.pipe';

describe('ValcomponentPipe', () => {
  it('create an instance', () => {
    const pipe = new ValcomponentPipe();
    expect(pipe).toBeTruthy();
  });
});
